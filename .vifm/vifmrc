" The environment variable $VIFM, if not explicitly set, is FIRST assumed to
" be, if existent,
"       $HOME/.vifm
" and would thus be equal to:
"       ~/.vifm
" if $HOME has not been set otherwise and the said location exists.

source $VIFM/conf/main.vifm

" vim: filetype=vifm
